# Kimi AskUserQuestion extract

Kimi ACP `handleQuestion` only forwards question 0 (`q0_opt_N`). Extra
questions must be proven against a real wire, not guessed.

```bash
python3 sandbox/kimi_ask/check_host.py
```
